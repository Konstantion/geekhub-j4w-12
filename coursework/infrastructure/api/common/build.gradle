dependencies {
    implementation project(':coursework:domain')
    implementation libsUtils.mapstruct

    annotationProcessor libsUtils.mapstructProcessor
    annotationProcessor libsUtils.lombokMapstructBinding
}